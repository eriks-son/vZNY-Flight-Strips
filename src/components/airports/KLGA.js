export const DPs = ["LGA7", "TNNIS6", "GLDMN8", "NTHNS5", "JUTES3", "HOPEA3"]

export function getDP(strip, config, type) {
    if (config === "Dep 4/Land Any") return DP[0];
    else if (config === "Dep 13/Land 22 (Coney)") {
        if ()
    }
    || config === "Dep 13/Land 22 (No Coney)"
    || config === "Dep 13/Land 4 (Coney)"
    || config === "Dep 13/Land 4 (No Coney)"
        
    }
}


if (strip.flight_plan.route.includes("NEWEL")
        || strip.flight_plan.route.includes("ELIOT")
        || strip.flight_plan.route.includes("ZIMMZ")
        || strip.flight_plan.route.includes("PARKE")
        || strip.flight_plan.route.includes("LANNA")
        || strip.flight_plan.route.includes("BIGGY")) return ""